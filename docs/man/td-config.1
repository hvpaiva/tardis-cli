.\" Automatically generated by Pandoc 3.6.4
.\"
.TH "TD\-CONFIG" "1" "2026" "TARDIS Manual"
.SH NAME
td\-config \- manage the TARDIS configuration file
.SH SYNOPSIS
\f[B]td config\f[R] \f[I]SUBCOMMAND\f[R]
.SH DESCRIPTION
\f[B]td config\f[R] provides subcommands to inspect and manage the
TARDIS configuration file.
The config file is TOML\-formatted and is created automatically on first
run with commented examples for every field.
.SH SUBCOMMANDS
.TP
\f[B]path\f[R]
Print the full path to the configuration file.
.TP
\f[B]show\f[R]
Display the effective configuration (format, timezone, and all defined
format presets).
.TP
\f[B]edit\f[R]
Open the configuration file in the editor specified by the
\f[B]EDITOR\f[R] environment variable (falls back to \f[B]vi\f[R] if
unset).
.TP
\f[B]presets\f[R]
List all user\-defined format presets (name and strftime pattern).
.SH EXAMPLES
Show the path to the config file:
.IP
.EX
td config path
.EE
.PP
Display effective configuration:
.IP
.EX
td config show
.EE
.PP
Edit the configuration file:
.IP
.EX
td config edit
.EE
.PP
List all format presets:
.IP
.EX
td config presets
.EE
.PP
Open with a specific editor:
.IP
.EX
EDITOR=nano td config edit
.EE
.SH ENVIRONMENT
.TP
\f[B]EDITOR\f[R]
Editor program used by \f[B]td config edit\f[R].
Defaults to \f[B]vi\f[R].
.TP
\f[B]XDG_CONFIG_HOME\f[R]
Override the configuration directory base path.
.SH FILES
.TP
\f[I]$XDG_CONFIG_HOME/tardis/config.toml\f[R]
Configuration file.
Platform defaults when XDG_CONFIG_HOME is unset:
.RS
.IP \[bu] 2
Linux: \f[I]\[ti]/.config/tardis/config.toml\f[R]
.IP \[bu] 2
macOS: \f[I]\[ti]/Library/Application Support/tardis/config.toml\f[R]
.IP \[bu] 2
Windows: \f[I]%APPDATA%\[rs]tardis\[rs]config.toml\f[R]
.RE
.SH SEE ALSO
\f[B]td\f[R](1), \f[B]td\-diff\f[R](1), \f[B]td\-convert\f[R](1),
\f[B]td\-tz\f[R](1), \f[B]td\-info\f[R](1), \f[B]td\-range\f[R](1),
\f[B]td\-completions\f[R](1)
.SH AUTHORS
TARDIS Contributors.
